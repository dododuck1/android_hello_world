buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.2' // ใช้เวอร์ชันที่ตรงกับ Gradle ที่ติดตั้ง
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
